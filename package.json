{
  "name": "workout-tracker",
  "version": "1.0.0",
  "description": "Workout Tracker application",
  "scripts": {
    "postinstall": "npm install --prefix ./Develop",
    "seed": "npm run seed --prefix ./Develop",
    "start": "npm start --prefix ./Develop"
  }
}